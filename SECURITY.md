# 記事の読み込みとセキュリティ

JSON・HTML・自動保存を復元するときは、記事本文とパーツ設定内のHTMLを検査する。
再描画とHTML出力にも同じ検査を適用し、イベント属性、危険なURL、未許可の埋め込みなどを除去する。
旧版の記事でも危険な要素は復元しない。通常の装飾・表・画像URL・パーツ設定は維持する。

読み込んだ記事（Markdownを含む）の外部画像・動画は、記事上部の「外部コンテンツを読み込む」を押すまで停止する。
停止中でもJSON保存・HTML出力には検査済みのURLを保持する。別の記事を読み込むと再び停止する。
読み込みを許可した後や、URLを自分で入力した新規記事では、外部サービスに通信する。
公開先のProtoPedia上の通信・サニタイズは、このエディターの設定では制御できない。

CSSの外部リソース参照、旧式の背景画像属性、固定配置、z-indexは記事の検査時に除去する。
プレビューは独立した描画領域に収め、記事の要素がエディターの外を覆うのを防ぐ。
読み込んだ記事の任意のCSSや実行スクリプトを完全に再現することは意図しない。

読込文字列・JSONファイルは5MB以下に制限する。JSONは2万項目・64階層、HTMLは2万要素・
128階層、Markdownは2万改行を上限とし、変換後のデータにも検査を行う。
上限超過は読み込みエラーとして扱う。埋め込みURLは遅延読込属性から復元する場合も、
実際に表示するURLをサービスの許可リストで検査する。
Xの表示を許可した後は、Xの公式スクリプトがエディターと同じページで動作する依存が残る。

## 依存ライブラリーと公開

HTMLの検査には [DOMPurify 3.4.15](https://github.com/cure53/DOMPurify/releases/tag/3.4.15) を使用する。
公式配布物を `vendor/purify.min.js` に同梱しており、実行時にCDNから取得しない。
ライセンスは `vendor/DOMPurify-LICENSE`。更新時は公式のセキュリティリリースを確認する。

公開時には `index.html` だけでなく `security.js`、`external-services.js`、`vendor/` も一緒に配置する。
サニタイザーを利用できない場合、未検査のHTMLを代わりに表示しない。

## 検証

`python -m unittest discover -s tests`

`tests/test_security.py` は、外部通信を遮断したブラウザーでJSON・HTML・復元データの攻撃文字列、
パーツ設定経由の再実行、外部通信の許可前後、URL保持、画面を覆うスタイルを確認する。
この対策は今回確認した経路への防御であり、将来の外部サービスや依存ライブラリーの無欠陥を保証しない。
